package main

/*
func TestParsingRomlinkFile(t *testing.T) {
	pathToRomfile = "testdata/usa.rom"
	romlinkFile, err := os.Open(pathToRomfile)
	if err != nil {
		t.Fatal("failed to open romlink file")
	}

	b, err := ioutil.ReadAll(romlinkFile)
	if err != nil {
		t.Fatal("failed to read romlink file")
	}

	compileRomlinkInput(string(b))
}
*/
